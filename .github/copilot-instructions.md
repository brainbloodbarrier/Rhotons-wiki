# Copilot Instructions

This project uses `AGENTS.md` (at the repository root) as the single source of truth for agent-facing instructions. GitHub Copilot should read that file.

Do not add content here — keep this stub as a pointer so instructions never drift between locations.

See: [AGENTS.md](../AGENTS.md)
